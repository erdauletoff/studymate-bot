# StudyMate Bot - Полное описание проекта

## 📋 Оглавление
1. [Обзор проекта](#обзор-проекта)
2. [Технологический стек](#технологический-стек)
3. [Структура проекта](#структура-проекта)
4. [База данных](#база-данных)
5. [Основные функции](#основные-функции)
6. [Архитектура и паттерны](#архитектура-и-паттерны)
7. [Локализация](#локализация)
8. [Развертывание](#развертывание)
9. [Недавние изменения](#недавние-изменения)

---

## Обзор проекта

**StudyMate** - это Telegram бот для образования, созданный для взаимодействия между менторами (учителями) и студентами.

### Основная функциональность:
- 👨‍🏫 **Менторы**: загружают учебные материалы, создают квизы, отвечают на вопросы студентов
- 👨‍🎓 **Студенты**: скачивают материалы, проходят квизы, задают анонимные вопросы
- 🌐 **Мультиязычность**: поддержка русского, каракалпакского и английского языков
- 📝 **Квизы**: создание тестов с таймером, автоматическая проверка, статистика

### Ключевые особенности:
- Анонимные вопросы от студентов
- Система квизов с таймером (20 секунд на вопрос, 7 минут на всю сессию)
- Возможность повторного прохождения квизов (в рейтинг идет только первая попытка)
- Детальная статистика по материалам и квизам
- Экспорт результатов в CSV
- Архивирование квизов
- Перезапуск квизов с перемешиванием вопросов

---

## Технологический стек

### Backend
- **Django 4.2+** - веб-фреймворк для управления данными
- **SQLite** (локально) / **PostgreSQL** (продакшен)
- **Django Admin** - административная панель

### Bot
- **aiogram 3.10+** - асинхронный фреймворк для Telegram Bot API
- **FSM (Finite State Machine)** - управление состояниями диалогов
- **asyncio** - асинхронное программирование

### Дополнительно
- **python-dotenv** - управление переменными окружения
- **psycopg2-binary** - PostgreSQL драйвер
- **dj-database-url** - парсинг DATABASE_URL
- **whitenoise** - обслуживание статических файлов
- **gunicorn** - WSGI сервер для продакшена

---

## Структура проекта

```
studymate-bot/
├── backend/                    # Django приложения
│   ├── core/                   # Основные настройки Django
│   │   ├── settings.py         # Конфигурация проекта
│   │   ├── urls.py             # URL маршруты
│   │   └── wsgi.py             # WSGI конфигурация
│   ├── mentors/                # Модели менторов
│   │   ├── models.py           # Mentor модель
│   │   └── admin.py            # Админка для менторов
│   ├── students/               # Модели студентов
│   │   ├── models.py           # Student модель
│   │   └── admin.py            # Админка для студентов
│   ├── materials/              # Учебные материалы
│   │   ├── models.py           # Topic, Material модели
│   │   └── admin.py            # Админка для материалов
│   ├── questions/              # Анонимные вопросы
│   │   ├── models.py           # Question модель
│   │   └── admin.py            # Админка для вопросов
│   ├── downloads/              # Отслеживание скачиваний
│   │   ├── models.py           # Download модель
│   │   └── admin.py            # Админка для скачиваний
│   └── quizzes/                # Система квизов
│       ├── models.py           # Quiz, QuizQuestion, QuizAttempt, QuizAnswer
│       └── admin.py            # Админка для квизов
├── bot/                        # Telegram бот
│   ├── handlers/               # Обработчики команд и сообщений
│   │   ├── __init__.py         # Регистрация роутеров
│   │   ├── start.py            # /start, выбор языка
│   │   ├── mentor.py           # Функции ментора
│   │   ├── student.py          # Функции студента
│   │   ├── questions.py        # Анонимные вопросы
│   │   └── quiz.py             # Система квизов (1435 строк)
│   ├── keyboards/              # Клавиатуры и кнопки
│   │   └── menus.py            # Все клавиатуры (mentor_menu, student_menu)
│   ├── utils/                  # Утилиты
│   │   └── quiz_parser.py      # Парсер .txt файлов с квизами
│   ├── db.py                   # Все функции работы с БД (@sync_to_async)
│   ├── texts.py                # Все тексты на 3 языках
│   └── middleware.py           # Middleware для проверки студент-ментор
├── run_bot.py                  # Точка входа для запуска бота
├── manage.py                   # Django management команды
├── requirements.txt            # Python зависимости
├── .env                        # Переменные окружения (не в git)
├── Procfile                    # Для развертывания на Heroku
├── runtime.txt                 # Версия Python для Heroku
└── CLAUDE.md                   # Инструкции по реализации квизов

```

---

## База данных

### 📊 Модели данных

#### 1. Mentor (Ментор)
```python
telegram_id: BigInteger (unique)  # Telegram ID ментора
name: String(100)                 # Имя ментора
group_chat_id: BigInteger         # ID группового чата
language: String(5)               # ru/qq/en
is_active: Boolean                # Активен ли
created_at: DateTime              # Дата создания
```

#### 2. Student (Студент)
```python
telegram_id: BigInteger (unique)  # Telegram ID студента
username: String(100)             # Username (может быть пустым)
first_name: String(100)           # Имя
last_name: String(100)            # Фамилия
mentor: ForeignKey(Mentor)        # Привязка к ментору
language: String(5)               # ru/qq/en
joined_at: DateTime               # Дата регистрации
last_active: DateTime             # Последняя активность
```

#### 3. Topic (Тема материалов)
```python
mentor: ForeignKey(Mentor)        # Владелец темы
name: String(100)                 # Название темы
created_at: DateTime              # Дата создания
```

#### 4. Material (Учебный материал)
```python
topic: ForeignKey(Topic)          # Тема
title: String(200)                # Название материала
file_id: String(200)              # Telegram file_id
file_type: String(20)             # document/photo/video/audio
uploaded_at: DateTime             # Дата загрузки
```

#### 5. Download (Скачивание)
```python
student: ForeignKey(Student)      # Кто скачал
material: ForeignKey(Material)    # Что скачал
downloaded_at: DateTime           # Когда скачал
```

#### 6. Question (Анонимный вопрос)
```python
student: ForeignKey(Student)      # Автор (скрыт от ментора)
mentor: ForeignKey(Mentor)        # Ментор получатель
text: Text                        # Текст вопроса
is_answered: Boolean              # Отвечен ли
created_at: DateTime              # Дата создания
```

#### 7. Quiz (Квиз)
```python
mentor: ForeignKey(Mentor)        # Создатель квиза
title: String(200)                # Название
topic: String(100)                # Тема (опционально)
is_active: Boolean                # Активен ли (архивирован или нет)
created_at: DateTime              # Дата создания
```

#### 8. QuizQuestion (Вопрос квиза)
```python
quiz: ForeignKey(Quiz)            # Квиз
question_text: Text               # Текст вопроса
option_a: String(500)             # Вариант A
option_b: String(500)             # Вариант B
option_c: String(500)             # Вариант C
option_d: String(500)             # Вариант D
correct_answer: String(1)         # A/B/C/D
order: Integer                    # Порядковый номер
```

#### 9. QuizAttempt (Попытка прохождения квиза)
```python
student: ForeignKey(Student)      # Студент
quiz: ForeignKey(Quiz)            # Квиз
score: Integer                    # Набрано баллов
total: Integer                    # Всего вопросов
started_at: DateTime              # Начало прохождения
finished_at: DateTime             # Окончание прохождения
```
**Важно:**
- Студент может проходить квиз неограниченное количество раз
- В рейтинге и статистике учитывается только **первая попытка** каждого студента
- Студент видит результаты своей последней попытки

#### 10. QuizAnswer (Ответ на вопрос)
```python
attempt: ForeignKey(QuizAttempt)  # Попытка
question: ForeignKey(QuizQuestion)# Вопрос
selected_answer: String(1)        # A/B/C/D или "-" (таймаут)
is_correct: Boolean               # Правильный ли ответ
```

---

## Основные функции

### 🎯 Для менторов

#### 1. Управление материалами
- **Создание тем** - организация материалов по темам (HTML, CSS, JavaScript и т.д.)
- **Загрузка файлов** - поддержка документов, фото, видео, аудио
- **Управление** - просмотр и удаление материалов/тем
- **Статистика** - кто и что скачивал, популярные материалы

#### 2. Система квизов
- **Загрузка квизов** - парсинг .txt файлов с вопросами
- **Управление вопросами** - добавление, редактирование, удаление
- **Просмотр результатов** - статистика, топ студентов, средний балл
- **Экспорт** - выгрузка результатов в CSV
- **Архивирование** - перевод квиза в неактивное состояние

#### 3. Анонимные вопросы
- **Просмотр вопросов** - список неотвеченных вопросов
- **Отметка "Отвечено"** - маркировка обработанных вопросов
- **Статистика** - количество вопросов

### 👨‍🎓 Для студентов

#### 1. Учебные материалы
- **Просмотр по темам** - организованный доступ к материалам
- **Скачивание** - получение файлов от ментора
- **История** - все скачанные материалы отслеживаются

#### 2. Квизы
- **Список квизов** - активные квизы с отметкой о прохождении
- **Прохождение** - ответы на вопросы с таймером (20 сек/вопрос)
- **Автосброс сессии** - если квиз не завершен в течение 7 минут, состояние автоматически сбрасывается
- **Повторное прохождение** - можно проходить квиз неограниченное количество раз
- **Результаты** - балл, средний по группе (по первым попыткам), детальный разбор всех ответов
- **Просмотр ответов** - в любой момент можно вернуться к своим ответам
- **Пинирование** - текущий вопрос закрепляется в чате

#### 3. Анонимные вопросы
- **Задать вопрос** - полная анонимность
- **Отправка ментору** - вопрос пересылается в групповой чат

---

## Архитектура и паттерны

### 🔧 Ключевые паттерны

#### 1. Асинхронные вызовы БД
Все обращения к Django ORM обернуты в `@sync_to_async`:
```python
@sync_to_async
def get_mentor_by_telegram_id(telegram_id: int):
    return Mentor.objects.get(telegram_id=telegram_id)
```

#### 2. Централизованная локализация
Все тексты в `bot/texts.py`:
```python
TEXTS = {
    "ru": {"key": "Русский текст"},
    "qq": {"key": "Qaraqalpaq tekst"},
    "en": {"key": "English text"}
}

def t(key: str, lang: str = None, **kwargs) -> str:
    # Возвращает локализованный текст с подстановкой параметров
```

#### 3. Мультиязычная обработка кнопок
Кнопки проверяются по всем языкам:
```python
@router.message(F.text.in_(["📤 Загрузить", "📤 Júklew", "📤 Upload"]))
async def upload_handler(message: Message):
    # Обработчик сработает на любом языке
```

#### 4. FSM для многошаговых диалогов
Использование aiogram FSM:
```python
class QuizStates(StatesGroup):
    waiting_quiz_file = State()
    waiting_quiz_confirm = State()
    taking_quiz = State()
```

#### 5. Middleware для проверки доступа
Проверка привязки студент-ментор:
```python
class StudentMentorCheckMiddleware(BaseMiddleware):
    async def __call__(self, handler, event, data):
        # Проверяет, что студент привязан к ментору
```

### ⚙️ Особенности реализации квизов

#### Таймер на вопросы
- **20 секунд** на каждый вопрос
- **Обратный отсчет** обновляется каждую секунду
- **Автопропуск** при истечении времени
- **Отмена таймера** при ответе студента

#### Управление таймерами
```python
# Хранение активных таймеров вопросов
active_timers = {
    attempt_id: (timeout_task, countdown_task)
}

# Хранение таймеров сессий (7 минут)
session_timers = {
    attempt_id: session_timeout_task
}

# Отмена при ответе
if attempt_id in active_timers:
    timeout_task, countdown_task = active_timers[attempt_id]
    timeout_task.cancel()
    countdown_task.cancel()

# Отмена таймера сессии при завершении квиза
if attempt_id in session_timers:
    session_timers[attempt_id].cancel()
    del session_timers[attempt_id]
```

#### Пагинация
- **Вопросы** - 5 на страницу
- **Ответы в разборе** - 10 на страницу
- Кнопки навигации: ◀️ 1/3 ▶️

#### Формат квиз-файла
```
Тема: HTML

1. Какой тег создаёт ссылку?
A) <link>
B*) <a>
C) <href>
D) <url>
```
Правильный ответ отмечается звездочкой: `B*)`

#### Безопасность от HTML-инъекций
Все тексты экранируются:
```python
def escape_html(text: str) -> str:
    return html.escape(text)
```

#### Система повторного прохождения квизов
- **Студент может проходить квиз неограниченное количество раз**
- **Последняя попытка** - `get_student_attempt()` возвращает самую свежую попытку
- **Первая попытка** - `get_student_first_attempt()` используется для статистики
- **Статистика** - все функции (`get_quiz_stats`, `get_quiz_average_score`, `get_quiz_top_students`) учитывают только первую попытку каждого студента
- **Кнопка "Пройти заново"** - доступна после просмотра результатов

#### Автоматический сброс сессии квиза
- **Таймер на 7 минут** запускается при начале квиза
- **Автоматическая очистка состояния** если квиз не завершен
- **Освобождение доступа** к другим функциям бота
- **Отмена всех таймеров** вопросов при сбросе сессии
- **Откепление сообщения** квиза из чата

---

## Локализация

### Поддерживаемые языки
1. **ru** - Русский
2. **qq** - Каракалпакский (Qaraqalpaq)
3. **en** - English

### Структура текстов
Файл `bot/texts.py` содержит:
- Приветствия и сообщения об ошибках
- Названия кнопок меню
- Сообщения загрузки/управления
- Тексты квизов и результатов
- Подтверждения удаления
- Статистика

### Переключение языка
- При первом запуске `/start`
- Через кнопку "🌐 Язык" в меню
- Сохраняется в профиле пользователя

---

## Развертывание

### Локальная разработка

1. **Клонирование репозитория**
```bash
git clone <repo-url>
cd studymate-bot
```

2. **Создание виртуального окружения**
```bash
python -m venv .venv
source .venv/bin/activate  # Linux/Mac
.venv\Scripts\activate     # Windows
```

3. **Установка зависимостей**
```bash
pip install -r requirements.txt
```

4. **Настройка переменных окружения**
Создать файл `.env`:
```
BOT_TOKEN=your_telegram_bot_token
SECRET_KEY=your_django_secret_key
DEBUG=True
DATABASE_URL=sqlite:///db.sqlite3
```

5. **Миграции базы данных**
```bash
python manage.py makemigrations
python manage.py migrate
```

6. **Создание суперпользователя**
```bash
python manage.py createsuperuser
```

7. **Настройка ментора**
```bash
python setup_mentor.py
```

8. **Запуск бота**
```bash
python run_bot.py
```

### Продакшен (Heroku)

1. **Создание приложения**
```bash
heroku create your-app-name
```

2. **Настройка переменных**
```bash
heroku config:set BOT_TOKEN=your_token
heroku config:set SECRET_KEY=your_secret
heroku config:set DEBUG=False
```

3. **Добавление PostgreSQL**
```bash
heroku addons:create heroku-postgresql:hobby-dev
```

4. **Деплой**
```bash
git push heroku main
```

5. **Миграции**
```bash
heroku run python manage.py migrate
```

### Файлы для деплоя
- **Procfile**: `web: gunicorn backend.core.wsgi`
- **runtime.txt**: `python-3.11.0`
- **requirements.txt**: все зависимости

---

## Недавние изменения

### Последние обновления (2026-01-29)

#### Система повторного прохождения квизов
- ✅ **Неограниченные попытки** - студенты могут проходить квизы несколько раз
- ✅ **Рейтинг по первой попытке** - в статистике учитывается только первая попытка
- ✅ **Кнопка "Пройти заново"** - добавлена возможность перепройти квиз после просмотра результатов
- ✅ **Обновлены функции БД** - `get_student_attempt()`, `get_student_first_attempt()`, `get_quiz_stats()`, `get_quiz_average_score()`, `get_quiz_top_students()`
- ✅ **Локализация** - добавлены переводы кнопки "Пройти заново" на 3 языка

**Файлы:**
- `bot/db.py` - обновлены функции статистики
- `bot/handlers/quiz.py` - убрана блокировка повторного прохождения
- `bot/texts.py` - добавлен ключ `quiz_retake`

#### Автоматический сброс сессии квиза (7 минут)
- ✅ **Таймер сессии** - `QUIZ_SESSION_TIMEOUT = 420` секунд (7 минут)
- ✅ **Автосброс состояния** - FSM state очищается через 7 минут неактивности
- ✅ **Освобождение бота** - другие функции становятся доступны после таймаута
- ✅ **Управление таймерами** - новый словарь `session_timers` для отслеживания
- ✅ **Функция `quiz_session_timeout()`** - асинхронная задача для автосброса

**Файлы:**
- `bot/handlers/quiz.py` - добавлена система таймеров сессий

### Последние коммиты

#### 4b4b4da - Add quiz restart feature with question shuffling and fix MESSAGE_TOO_LONG error
- Добавлена функция перезапуска квизов
- Перемешивание вопросов при перезапуске
- Исправлена ошибка MESSAGE_TOO_LONG (слишком длинное сообщение)
- Добавлена пагинация для просмотра ответов

#### 759b31b - update bot quiz timing
- Обновлен таймер квизов
- Установлен таймаут 20 секунд на вопрос
- Улучшен обратный отсчет

#### 2bb947b - ver 1
- Базовая версия проекта

#### 85bec6c - Initial commit
- Первый коммит проекта

### Текущее состояние (изменены файлы)
```
M .claude/settings.local.json  # Настройки Claude AI
M CLAUDE.md                     # Инструкции по проекту
M about.md                      # Этот файл (обновлена документация)
M bot/handlers/quiz.py          # Обработчик квизов (таймеры + повторное прохождение)
M bot/db.py                     # Функции БД (статистика по первым попыткам)
M bot/texts.py                  # Локализация (кнопка "Пройти заново")
```

---

## 🔍 Детали реализации

### Система квизов (bot/handlers/quiz.py)

**Размер:** 1435 строк кода

**Основные компоненты:**

1. **Состояния FSM**
   - `QuizStates` - для прохождения квизов
   - `QuizManageStates` - для управления вопросами

2. **Константы**
   - `QUESTION_TIMEOUT = 20` - секунд на вопрос
   - `QUIZ_SESSION_TIMEOUT = 420` - секунд (7 минут) на всю сессию квиза
   - `QUESTIONS_PER_PAGE = 5` - вопросов на страницу
   - `ANSWERS_PER_PAGE = 10` - ответов в разборе

3. **Обработчики ментора**
   - Загрузка квизов из .txt
   - Управление квизами (архив/разархив)
   - Редактирование вопросов
   - Экспорт результатов в CSV
   - Просмотр статистики

4. **Обработчики студента**
   - Прохождение квизов с таймером
   - Просмотр результатов
   - Просмотр ответов (пагинация)

5. **Вспомогательные функции**
   - `escape_html()` - защита от HTML-инъекций
   - `build_review_text()` - формирование разбора ответов
   - `show_question()` - отображение вопроса с таймером
   - `update_countdown()` - обновление обратного отсчета
   - `question_timeout()` - обработка таймаута

### База данных (bot/db.py)

**Все функции обернуты в `@sync_to_async`**

**Категории функций:**

1. **Язык** - `get_user_language()`, `set_user_language()`
2. **Менторы** - `get_mentor_by_telegram_id()`, `is_mentor()`
3. **Студенты** - `get_or_create_student()`, `get_student_mentor()`
4. **Темы и материалы** - CRUD операции
5. **Вопросы** - создание, получение, маркировка
6. **Скачивания** - отслеживание
7. **Квизы** - полный CRUD
8. **Попытки квизов** - создание, завершение, получение
9. **Ответы на вопросы квизов** - сохранение, получение

### Парсер квизов (bot/utils/quiz_parser.py)

**Функция:** `parse_quiz_file(content: str) -> dict`

**Возвращает:**
```python
{
    "title": "HTML",
    "topic": "HTML",
    "questions": [
        {
            "text": "Какой тег создаёт ссылку?",
            "option_a": "<link>",
            "option_b": "<a>",
            "option_c": "<href>",
            "option_d": "<url>",
            "correct": "B"
        }
    ]
}
```

**Правила парсинга:**
- Первая строка "Тема: XXX" → название
- Вопросы начинаются с "1.", "2.", и т.д.
- Варианты: A), B), C), D)
- Правильный ответ: B*)

---

## 📝 Важные замечания

### Для разработчиков

1. **Всегда используйте @sync_to_async** для Django ORM
2. **Проверяйте FSM state** перед операциями с состоянием
3. **Экранируйте HTML** в текстах для parse_mode="HTML"
4. **Добавляйте тексты на все языки** при изменении UI
5. **Используйте F.text.in_([...])** для мультиязычных кнопок

### Ограничения Telegram

1. **Максимум 4096 символов** в одном сообщении
   - Решение: пагинация длинных текстов
2. **Максимум 64 байта** в callback_data
   - Решение: короткие идентификаторы
3. **Максимум 8 кнопок** в одном ряду InlineKeyboard
   - Решение: несколько рядов

### Безопасность

1. **SECRET_KEY** - хранится в .env (не в git)
2. **BOT_TOKEN** - хранится в .env (не в git)
3. **HTML-инъекции** - все тексты экранируются
4. **SQL-инъекции** - используется Django ORM
5. **Доступ** - middleware проверяет привязку студент-ментор

---

## 🚀 Возможные улучшения

### Функциональность
- [ ] Групповые чаты для обсуждений
- [ ] Push-уведомления о новых материалах
- [ ] Избранные материалы
- [ ] Расписание занятий
- [ ] Домашние задания с проверкой
- [ ] Видео-уроки
- [ ] Сертификаты по завершению курса

### Квизы
- [x] Повторное прохождение квизов (реализовано)
- [x] Автосброс сессии через 7 минут (реализовано)
- [ ] Разные типы вопросов (множественный выбор, текстовый ответ)
- [ ] Категории сложности
- [ ] Адаптивные квизы
- [ ] Система рейтинга студентов (в разработке)
- [ ] Дедлайны для квизов

### Техническое
- [ ] Redis для FSM storage
- [ ] Celery для фоновых задач
- [ ] Docker контейнеризация
- [ ] CI/CD pipeline
- [ ] Логирование и мониторинг
- [ ] Unit и integration тесты
- [ ] API для интеграции с другими системами

---

## 📞 Контакты и поддержка

### Структура команды
- Backend: Django + PostgreSQL
- Bot: aiogram 3.x
- Deployment: Heroku

### Git репозиторий
- Текущая ветка: `main`
- Последний коммит: `4b4b4da`

### Файловая структура
- **Backend**: 6 Django apps (core, mentors, students, materials, questions, downloads, quizzes)
- **Bot**: 4 основных handler'а + middleware + клавиатуры + утилиты
- **База данных**: SQLite (локально) / PostgreSQL (продакшен)

---

## 📚 Дополнительная документация

### Django Admin
URL: `/admin/`

**Доступные разделы:**
- Mentors - управление менторами
- Students - управление студентами
- Topics - темы материалов
- Materials - учебные материалы
- Questions - анонимные вопросы
- Downloads - история скачиваний
- Quizzes - квизы
- Quiz Questions - вопросы квизов
- Quiz Attempts - попытки прохождения
- Quiz Answers - ответы студентов

### Telegram Bot Commands
- `/start` - Запуск бота, выбор языка
- Остальное через кнопки меню

### Environment Variables
```
BOT_TOKEN=              # Telegram Bot API Token
SECRET_KEY=             # Django Secret Key
DEBUG=                  # True/False
DATABASE_URL=           # Database connection string
```

---

**Версия документа:** 1.0
**Дата создания:** 2026-01-29
**Статус проекта:** Активная разработка

---

## Выводы

StudyMate Bot - это полнофункциональная образовательная платформа на базе Telegram с:
- ✅ Мультиязычной поддержкой (ru/qq/en)
- ✅ Системой управления материалами
- ✅ Интерактивными квизами с таймером
- ✅ Анонимными вопросами
- ✅ Детальной статистикой
- ✅ Django Admin панелью
- ✅ Масштабируемой архитектурой

Проект готов к развертыванию и дальнейшему расширению функциональности.
